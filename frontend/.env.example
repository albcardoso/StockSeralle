# ── Variáveis de Ambiente — StockSync Frontend ──────────────────────────────
# Copie este arquivo como .env.local para desenvolvimento local
# Na Vercel: defina estas variáveis em Project Settings → Environment Variables

# URL do backend .NET (Railway em produção, localhost em dev)
API_URL=http://localhost:5000

# Exemplo em produção:
# API_URL=https://stocksync-backend.up.railway.app
